@model BlogApplication.Models.ViewModels.Article.ArticleCardListViewModel

<div class="container mt-4">
    <div class="col-md-12">
        <div class="row">
            @foreach (var article in Model.Articles)
            {
                @Html.DisplayFor(t => article, "Article/ArticleCardViewModel")
            }
        </div>
    </div>
</div>